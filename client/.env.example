# React Frontend Environment Configuration
# This file is for development overrides only
# In production, the app will use relative paths through NGINX proxy

# Uncomment the line below ONLY if you need to override the automatic API detection
# For normal development with React dev server + FastAPI backend, leave this commented
# REACT_APP_API_BASE_URL=http://localhost:8000

# For testing with a different backend port, uncomment and modify:
# REACT_APP_API_BASE_URL=http://localhost:8001

# For testing against a remote development server:
# REACT_APP_API_BASE_URL=https://your-dev-api.example.com