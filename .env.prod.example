# Production Environment Configuration
# Copy this file to .env.prod and fill in your actual values

# Database Configuration (Local PostgreSQL)
# Update with your local database connection details
DATABASE_URL=postgresql://postgres:your_local_postgres_password@host.docker.internal:5432/ecommerce_db

# Alternative for Linux (if host.docker.internal doesn't work):
# DATABASE_URL=postgresql://postgres:your_local_postgres_password@172.17.0.1:5432/ecommerce_db

# JWT Configuration
SECRET_KEY=your_super_secure_secret_key_minimum_32_characters_long
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Environment
ENVIRONMENT=production

# Google Gemini API Key
GEMINI_API_KEY=your_gemini_api_key

# Production Domain
PRODUCTION_DOMAIN=gen-capstone.tanfamily.cc

# Custom Frontend Domain (optional)
# Set this if you want to use a different domain than the default
# FRONTEND_DOMAIN=my-custom-domain.com

# Additional Security (optional)
# SSL_CERT_PATH=/path/to/ssl/cert.pem
# SSL_KEY_PATH=/path/to/ssl/key.pem